# ── Discord Bot ──────────────────────────────────────────────
discord.py>=2.3.0

# ── Environment ──────────────────────────────────────────────
python-dotenv>=1.0.0

# ── HTTP & Web Scraping ──────────────────────────────────────
aiohttp>=3.8.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0                 # parser backend for BeautifulSoup(..., "lxml") in events.py / page_analyzer.py

# ── System ───────────────────────────────────────────────────
psutil>=5.9.0              # NOTE: not referenced anywhere in the codebase right now

# ── Machine Learning & Data ──────────────────────────────────
scikit-learn>=1.3.0
lightgbm>=4.0.0           # model.pkl is an LGBM pipeline — required by train.py & to unpickle in predict.py
shap>=0.44.0              # SHAP explainability in ai/predict.py
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# ── Image Processing / OCR ───────────────────────────────────
pillow>=10.0.0
pytesseract>=0.3.10

# ── API Service (app.py — FastAPI URL-prediction service) ────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# ── AI Agent (Gemini analysis layer for borderline cases) ────
google-genai>=1.0.0          # Google Gen AI SDK (replaces the deprecated google-generativeai)
python-whois>=0.9.0          # domain-age lookups during investigation

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
