# ── Discord Bot ──────────────────────────────────────────────
discord.py==2.7.1

# ── Environment ──────────────────────────────────────────────
python-dotenv==1.2.3

# ── HTTP & Web Scraping ──────────────────────────────────────
aiohttp==3.14.3
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3                   # parser backend for BeautifulSoup(..., "lxml") in events.py / page_analyzer.py

# ── Machine Learning & Data ──────────────────────────────────
scikit-learn==1.7.2           # must match the version model.pkl was trained with
lightgbm==4.6.0               # model.pkl is an LGBM pipeline — required by train.py & to unpickle in predict.py
shap==0.52.0                  # SHAP explainability in ai/predict.py
pandas==3.0.6
numpy==2.5.3
joblib==1.6.0

# ── Image Processing / OCR ───────────────────────────────────
pillow==12.3.0
pytesseract==0.3.13

# ── API Service (app.py — FastAPI URL-prediction service) ────
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5

# ── AI Agent (Gemini analysis layer for borderline cases) ────
google-genai==2.25.0          # Google Gen AI SDK (replaces the deprecated google-generativeai)
python-whois==0.9.6           # domain-age lookups during investigation
